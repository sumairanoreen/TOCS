pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Starting the build stage'
                echo 'Build the stage successfully'
            }
        }
        stage('Test') {
            steps {
                echo 'Starting the build stage'
                echo 'Build the stage successfully'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Starting the build stage'
                echo 'Build the stage successfully'
            }
        }
    }
}
